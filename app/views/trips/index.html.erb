<nav>
  <div class='btn1 btn1-6 btn1-6d' ng-click="tripShow = !tripShow">Trips</div> 
  <span ng-show="tripDetailsShow" ng-class='{tripDetails: tripDetailsShow}'>Description: {{currentTrip.description}}</span>
</nav>

<div class='row'>
  <div class='col-md-4'>
    <button class="tripbtn tripbtn-2 tripbtn-2d" ng-click="createTrip()">New Trip <i class="fa fa-plus"></i></button>


    <div ng-repeat="trip in trips">
       <button class='tripbtn tripbtn-1 tripbtn-1d' ng-click="tripClick(trip)"> {{trip.name}} <br>
        {{trip.destination}}</button>
    </div>
  </div>

  <div class='col-md-4' ng-show='newTrip' ng-include="templates[0].url">
  </div>

  <div class='col-md-4' ng-show="activitiesShow">
    <button class="tripbtn tripbtn-2 tripbtn-2d" ng-click="createActivity()">New Activity <i class="fa fa-plus"></i></button>
    <div ng-repeat="activity in activities">
      <button class='tripbtn tripbtn-1 tripbtn-1d' ng-click="tripClick(trip)"> {{activity.name}} <br>
          {{trip.destination}}</button>
    </div>
  </div>

  <div class='col-md-4' ng-show='newActivity' ng-include="templates[1].url">
  </div>

  <div class='col-md-4' ng-show="yelpShow">
    <div class='searchfield'>
      <form>
        <input type="text" placeholder="Search for places" ng-model="yelp.search"><button type="submit" class='btn btn-danger'  ng-click="yelpSearch()"><i class="fa fa-search"></i></button>
      </form>
    </div>

    <div ng-repeat="result in yelpResult" class='media yelpBox'>
      <div class='pull-left'>
        <img src={{result.image_url}} class='media-object img-circle' />
      </div>
      <div class='media-body'>
        <h4>{{result.name}}</h4>
        {{result.location}}
        {{result.url}}
        {{result.rating}}
      </div>

    </div>

  </div>



</div> <!--row close-->